# Build artifacts
build/**
.gradle/**
out/**

# Not strictly necessary, however, *.jar files are often ignored and this one is required by the Gradle wrapper.
!gradle/wrapper/gradle-wrapper.jar

# Editor settings
.idea/**
codemr/**


# Ignore the files modified before/after the simulation by the user
results.json
src/main/resources/custom.yml

# Ιgnore files that don't exist, but git thinks exist
/src/main/resources/*